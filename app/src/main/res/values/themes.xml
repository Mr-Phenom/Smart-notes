<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartNotes" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="My_Theme" parent="Theme.AppCompat.Light">
        <item name="android:titleTextColor">@color/black1</item>
        <item name="android:textColor">@color/black</item>
        <item name="backgroundColor">@color/dark_yellow</item>
        <item name="background">@color/dark_yellow</item>
        <item name="android:actionBarStyle">@style/actionbar</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="actionbar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/dark_yellow</item>
        
    </style>

    <style name="Theme.SmartNotes" parent="Base.Theme.SmartNotes" />
</resources>